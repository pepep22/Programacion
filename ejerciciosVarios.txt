2) namespace o
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int a;
            int q;
            int s;
            int r;
            bool p;
            string z;
            p = true;
            while (p == true) 
            {
                Console.WriteLine("Ingrese las medidas del perimetro del rectangulo a medir:");
                a = Convert.ToInt16(Console.ReadLine()); 
                q = Convert.ToInt16(Console.ReadLine());
                s = 2 * a + 2 * q;
                Console.WriteLine("Ingrese las medidas del area del rectangulo a medir:");
                a = Convert.ToInt16(Console.ReadLine());
                q = Convert.ToInt16(Console.ReadLine());
                r = a * q;
                Console.Write("El perimetro mide ");
                Console.WriteLine(s);
                Console.Write("El area mide ");
                Console.WriteLine(r);
                Console.WriteLine("¿Desea seguir calculando?");
                z = Console.ReadLine();
                if (z == "no") 
                {
                    p = false;
                }
            }
        }
    }
}

3)namespace o
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int a;
            int q;
            Console.WriteLine("¿Cuantos postulantes hay?");
            a = Convert.ToInt16(Console.ReadLine());
            Console.WriteLine("cuantos son mujeres?");
            q = Convert.ToInt16(Console.ReadLine());
            Console.Write("la cantidad de mujeres son: ");
            Console.WriteLine(q);
            Console.Write("la cantidad de hombres es: ");
            Console.WriteLine(a-q);
        }
    }
}
